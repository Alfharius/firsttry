# URL и anon key из Supabase: Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# true = только auth через localStorage (без Supabase Auth). Данные (мероприятия, профиль в БД) при этом всё равно читаются из Supabase, если заданы URL и ключ выше.
VITE_USE_MOCK=false
